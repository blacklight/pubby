# 02 — Framework Adapters — Follow-ups

- [ ] Add Django adapter (`django.py`) if there's demand.
- [ ] Consider async-native FastAPI routes (with `async def`) once the handler/storage layer supports async.
- [ ] Add WebSocket support for real-time streaming of inbox activities (framework-specific).
- [ ] Shared test base class or parametrized fixture to reduce duplication across adapter test files.
